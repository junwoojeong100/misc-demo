apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jp-fep.fullname" . }}-test-connection"
  labels:
    {{- include "jp-fep.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jp-fep.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
