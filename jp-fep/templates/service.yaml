apiVersion: v1
kind: Service
metadata:
  name: {{ include "jp-fep.fullname" . }}
  labels:
    {{- include "jp-fep.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jp-fep.selectorLabels" . | nindent 4 }}
